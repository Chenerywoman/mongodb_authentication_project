<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
</head>
<body>
    <h1>All Users</h1>
    <p>Only users with admin rights can view all users.</p>
    <div class="users">
        {{#each users}}
            <div class="user">
                <table>
                    <tr>
                        <td>name:</td>
                        <td>{{this.first_name}} {{this.surname}}</td>
                    </tr>
                    <tr>
                        <td>email:</td>
                        <td>{{this.email}}</td>
                    </tr>
                    <tr>
                        <td>id:</td>
                        <td>{{this.id}}</td>
                    </tr>
                </table>

                <form class="update" action="/update/{{this.id}}" method="POST">
                        <button type="submit">Update</button>
                </form>

                <form class="delete" action="/delete/{{this.id}}" method="POST">
                    <input class="user-to-delete" type="hidden" name="userName" value="{{this.first_name}} {{this.surname}}">
                    <button type="submit">Delete</button>
                </form>

            </div>
        {{/each}}
</body>
</html>