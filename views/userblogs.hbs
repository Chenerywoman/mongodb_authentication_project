<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your blogs</title>
</head>
<body>
    <h1>{{#if ownblogs}}Your {{else}} All {{/if}}blogs</h1>
    <div class="individual-blogs-container">
        {{#each blogs}}
        <div class="individual-blog">
            <h4>{{this.title}}</h4>
            <h5>author: {{this.user.first_name}} {{this.user.surname}}</h5>
            {{!-- <a href="/blog/{{this._id}}">{{this.title}}</a>  --}}
            <a href="/updateblog/{{this._id}}">change</a>

            <form action="/deleteblog/{{this._id}}" method="POST">
                <button type="submit">delete</button>
            </form>
        </div>
        {{/each}}
    </div>
</body>
</html>